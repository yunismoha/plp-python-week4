# PLP Python Week 4 - Functions & Toolbox

* `welcome.py` - Defines a welcome function that creates a greeting for different names.
* `toolbox.py` - Contains functions for doubling numbers, checking passes, and creating greetings.

The `greet` function was the hardest to write because it uses a default parameter. I had to understand how Python uses `"Hello"` automatically when no greeting is provided.
